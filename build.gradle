apply plugin: 'groovy'

repositories {
    mavenCentral()
}

dependencies {
    compile(
            'org.codehaus.groovy:groovy-all:2.3.6'
    )
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.0'
}
